import React from "react";
import { withRouter } from "react-router";

const Dark = () => {
    return (
        <div>
            <p>Dark Jokes</p>
        </div>
    );
};

export default withRouter(Dark);
