import React from "react";
import { withRouter } from "react-router";

const Random = () => {
    return (
        <div>
            <p>Random Jokes Here</p>
        </div>
    );
};

export default withRouter(Random);
