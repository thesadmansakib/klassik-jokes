import React from "react";
import { withRouter } from "react-router";

const Meme = () => {
    return (
        <div>
            <p>Meme Generator</p>
        </div>
    );
};

export default withRouter(Meme);
