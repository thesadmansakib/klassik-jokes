import React from "react";
import { withRouter } from "react-router";

const Friends = () => {
    return (
        <div>
            <p>Joke About Friends</p>
        </div>
    );
};

export default withRouter(Friends);
